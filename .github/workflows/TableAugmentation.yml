# This is a basic workflow to help you get started with Actions

name: TableAugmentation

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "master" branch
  push:
    branches: 
      - "test"

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "updateTables"
  updateTables:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v1
      
      # Runs a set of commands using the runners shell
      - name: Install and connect to Databricks-sql-cli
        env:
          access_token: ${{ secrets.DBSQLCLI_ACCESS_TOKEN }}
          host_name: ${{ secrets.DBSQLCLI_HOST_NAME }}
          http_path: ${{ DBSQLCLI_HTTP_PATH }}
        run: |
          python3 -m pip install databricks-sql-cli
          #dbsqlcli -e "SELECT * FROM default.diamonds LIMIT 2" \
          #  --hostname "dbc-a1b2345c-d6e78.cloud.databricks.com" \
          #  --http-path "/sql/1.0/warehouses/1abc2d3456e7f890a" \
          #  --access-token "dapi1234567890b2cd34ef5a67bc8de90fa12b"

          # cat ~/.dbsqlcli/dbsqlclirc  # This is the databricks config file that connects to the sql-warehouse-cluster. 
          # todo: need to connect to github secrets and pull the credentials. 
          # todo: connect tot the warehouse cluster
          # todo: check if tables exists within the cluster. -> perform short simple query. 
          # todo: check if commit file name starts with "create", ends with ".sql"
          
          echo Add other actions to build,
          echo test, and deploy your project.
          echo $access_token
          echo $host_name
          echo $http_path
